{
  "name": "gapwise-status",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run verify:status-scripts && astro build",
    "preview": "astro preview",
    "check": "npm run verify:status-scripts && astro check",
    "verify:status-scripts": "node --check scripts/update-status.mjs && node --check scripts/set-manual-status.mjs"
  },
  "dependencies": {
    "astro": "7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=22"
  }
}
